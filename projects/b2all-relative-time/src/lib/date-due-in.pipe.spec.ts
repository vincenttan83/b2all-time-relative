import { DateDueInPipe } from './date-due-in.pipe';

describe('DateDueInPipe', () => {
  it('create an instance', () => {
    const pipe = new DateDueInPipe();
    expect(pipe).toBeTruthy();
  });
});
